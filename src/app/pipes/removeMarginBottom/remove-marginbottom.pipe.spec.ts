import { RemoveMarginbottomPipe } from './remove-marginbottom.pipe';

describe('RemoveMarginbottomPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveMarginbottomPipe();
    expect(pipe).toBeTruthy();
  });
});
